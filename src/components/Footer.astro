---
interface Props {
  lang: 'en' | 'es';
  currentPath: string;
}

const { lang, currentPath } = Astro.props;
const year = new Date().getFullYear();

const base = lang === 'es' ? '/es/' : '/';
const isHome = currentPath === base;

/* Same rule as Header: anchors only work as anchors on the home page. */
const anchor = (hash: string) => (isHome ? `#${hash}` : `${base}#${hash}`);
---

<footer class="w-full border-t border-white/15 mt-24">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 md:px-8 py-10 grid gap-8 md:grid-cols-4 text-white/80">
    <div class="md:col-span-2">
      <div class="flex items-center gap-3 mb-4">
        <img src="/logo-mark.svg" alt="All Needs Discount LLC" class="h-8 w-8 shrink-0 object-contain" width="32" height="32" loading="lazy" />
        <span class="font-mono font-bold tracking-wider text-sm uppercase text-white">All Needs Discount</span>
      </div>
      <p class="text-sm text-white/60 leading-relaxed">
        {lang === 'es'
          ? 'Remodelación y construcción residencial en el sur de Georgia. Calidad, oficio y comunicación directa.'
          : 'Residential remodeling and construction in South Georgia. Quality craftsmanship and direct communication.'}
      </p>
    </div>

    <div>
      <h3 class="font-mono text-xs uppercase tracking-widest text-white mb-4">
        {lang === 'es' ? 'Contacto' : 'Contact'}
      </h3>
      <ul class="space-y-2 text-sm font-mono">
        <li>
          <a href={anchor('contact')} class="hover:text-white transition-colors">
            {lang === 'es' ? 'Solicitar cotización' : 'Request a quote'}
          </a>
        </li>
        <li>
          <a href="mailto:allneedsdiscount1@gmail.com" class="hover:text-white transition-colors break-all">
            allneedsdiscount1@gmail.com
          </a>
        </li>
        <li class="text-white/60">
          {lang === 'es' ? 'Ray City · Sur de Georgia' : 'Ray City · South Georgia'}
        </li>
      </ul>
    </div>

    <div>
      <h3 class="font-mono text-xs uppercase tracking-widest text-white mb-4">
        {lang === 'es' ? 'Navegación' : 'Navigation'}
      </h3>
      <ul class="space-y-2 text-sm font-mono">
        <li><a href={anchor('about')} class="hover:text-white transition-colors">{lang === 'es' ? 'Nosotros' : 'About'}</a></li>
        <li><a href={anchor('services')} class="hover:text-white transition-colors">{lang === 'es' ? 'Servicios' : 'Services'}</a></li>
        <li><a href={`${base}rentals/`} class="hover:text-white transition-colors">{lang === 'es' ? 'Renta de equipos' : 'Equipment Rental'}</a></li>
        <li><a href={anchor('area')} class="hover:text-white transition-colors">{lang === 'es' ? 'Área de servicio' : 'Service Area'}</a></li>
        <li><a href={anchor('contact')} class="hover:text-white transition-colors">{lang === 'es' ? 'Solicitar cotización' : 'Request a quote'}</a></li>
      </ul>
    </div>
  </div>

  <div class="border-t border-white/10">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 md:px-8 py-5 flex flex-col sm:flex-row items-center justify-between gap-2 text-[10px] font-mono uppercase tracking-widest text-white/50">
      <span>© {year} All Needs Discount LLC</span>
      <span>{lang === 'es' ? 'Licencia · Seguros al día' : 'Licensed · Insured'}</span>
    </div>
  </div>
</footer>
